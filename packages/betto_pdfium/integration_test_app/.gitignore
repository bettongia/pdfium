# Android JNI libraries — populated by scripts/fetch_mobile_binaries.sh
android/src/main/jniLibs/

# iOS xcframework lives in betto_pdfium_ios/ at the repo root (gitignored there)

# Flutter build outputs
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
pubspec.lock
